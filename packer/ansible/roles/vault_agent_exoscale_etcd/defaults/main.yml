---

vault_agent_exoscale_etcd_config_path: "{{ vault_config_path }}"
vault_agent_exoscale_etcd_user: "{{ vault_user }}"
vault_agent_exoscale_etcd_group: "{{ vault_group }}"

vault_agent_exoscale_etcd_auth_path: null
vault_agent_exoscale_etcd_paths:
  kv_instance_pool_exoscale_api: "kv/platform/exoscale/etcd-instance-pool"
  kv_backup_exoscale_api: "kv/platform/exoscale/etcd-backup"
  kv_backup_encryption: "kv/platform/backup/etcd-public"
  pki_etcd_cert: 'pki/platform/kubernetes/etcd/issue/server'

# TODO: make provider-specific sections optional
# vault_agent_kube_control_plane_provider: None | 'exoscale'
